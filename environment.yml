name: Masters-Thesis
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - seaborn
  - jupyter
  - xgboost
  - lightgbm
  - catboost
  - imbalanced-learn
  - pytorch
  - torchvision
  - torchaudio
  - transformers
  - nltk
  - spacy
  - shap
  - setuptools
  - ipykernel
  - json5
  - r-tableone
  - pip
  - pip:

      - openai>=1.0.0 # OpenAI API client

      # --------------------------------------------

      # Advanced ML
      - tensorflow

      # Model explainability
      - lime

      # Utilities
      - tqdm
      - joblib

      # MLOps / Experiment Tracking
      - mlflow

      # Visualization
      - plotly

      # APIs & Web Apps
      - flask

      # --------------------------------------------
      # 💡 Extra Recommendations (Uncomment if needed)
      # --------------------------------------------

      # Data & Big Data
      # sqlalchemy
      # dask
      # polars
      # pyarrow
      # vaex
      # modin[ray]
      # datatable
      # pyspark

      # Time Series
      - statsmodels
      # pmdarima
      # prophet
      # gluonts
      # darts

      # MLOps / Experiment Tracking
      # wandb
      # tensorboard
      # hydra-core
      # prefect
      # dagster

      # Deep Learning
      # pytorch-lightning
      # accelerate
      # keras
      # jax
      # flax

      # NLP
      - gensim
      # sentence-transformers
      # sumy
      # textblob
      # fuzzywuzzy

      # Visualization
      # bokeh
      # altair
      # geopandas
      # folium

      # Security & Config
      # cryptography
      # python-decouple
      # pydantic

      # Testing & CI/CD
      # pre-commit
      # black
      # flake8
      # pytest
      # pytest-cov
      # tox
      # mypy
      # bandit

      # Extra ML/DL
      # catboost
      # optuna
      # ray[tune]

      # Explainability & Fairness
      # fairlearn
      # alibi


      # - datasets # Hugging Face Datasets
      # - evaluate # Hugging Face Evaluate
      # - sentencepiece # Tokenizer for various models
      # - accelerate # For optimized model training
      # - bitsandbytes # 8-bit optimizers and quantization
      # - peft # Parameter-Efficient Fine-Tuning
      # - langchain # Framework for developing applications with LLMs
      # - chromadb # Vector database for embeddings
      # - faiss-cpu # Efficient similarity search and clustering of dense vectors
      # - gradio # Web-based UI for machine learning models
      # - wandb # Weights & Biases for experiment tracking
      # - optuna # Hyperparameter optimization framework
      # - fastapi # Modern, fast web framework for building APIs
      # - uvicorn # ASGI server for FastAPI
      # - python-multipart # For handling file uploads in FastAPI

